import sys
from struct import pack, unpack

class Instruction:
    def __init__(self, binary):
        # binConversion = hex(binary)
        # self.opcode = '{0:06b}'.format(binConversion)
        # self.
        pass